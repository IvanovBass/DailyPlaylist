<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DailyPlaylist.View.LegalPage"
             Title="LegalPage">
    <VerticalStackLayout Padding="0,15,0,0">

        <Picker x:Name="LanguagePicker" SelectedIndexChanged="OnLanguageChanged" HorizontalOptions="Center" HorizontalTextAlignment="Center"
                FontSize="Large">
            <Picker.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Français</x:String>
                    <x:String>English</x:String>
                </x:Array>
            </Picker.ItemsSource>
        </Picker>

        <ScrollView>
            <Label  x:Name="TermsOfUseLabel" Padding="15" HorizontalOptions="Center" HorizontalTextAlignment="Center">

            </Label>
        </ScrollView>
    </VerticalStackLayout>
</ContentPage>